name: .NET

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: build and deploy docker image
        uses: fifsky/ssh-action@master
        with:
          command: |
            cd /www/wwwroot/api.sinamn75.com
            git pull https://SinaMN75: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@github.com/BetterSoftCo/SinaMN75Api_aspnet.git
            git submodule update --remote -f
            docker-compose up -d --build --force-recreate
            /etc/init.d/nginx restart
          host: 45.139.11.86
          user: root
          pass: BetterSoft1234
          